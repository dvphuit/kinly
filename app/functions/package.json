{
  "name": "functions",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "test:crypto": "npm run build && node --test test/tokenEncryption.test.mjs",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,hosting",
    "deploy": "npm run build && firebase deploy --only functions,hosting",
    "lint": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.2.1",
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2",
    "googleapis": "^176.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  },
  "private": true,
  "engines": {
    "node": "20"
  }
}
